# Scripts

Build and content generation scripts will live here.

## Planned

- `generate-hsk-content.ts` — fetch/parse HSK word lists and generate JSON content for `src/data/`
- `generate-audio.ts` — batch-generate audio samples using AWS Polly
